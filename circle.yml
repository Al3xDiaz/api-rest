machine:
    -node:
        version: 12.18.3
    -services:
        -docker
dependecies:
    override:
        -dotnet test
depoyment:
    prod:
        branch: main
        commands:
            -sh commands/Linux.sh